spring.data.mongodb.uri=${MONGODB_URI:mongodb://localhost:27017}
spring.data.mongodb.database=${MONGODB_DATABASE:salesdb-test}
app.jwt.secret=${APP_JWT_SECRET:test-insecure-change-me-test-insecure-change-me-32bytes}
spring.mail.username=ignored
spring.mail.password=ignored
spring.mail.host=localhost
spring.mail.port=1025
app.mail.enabled=false
app.frontend.verify-url=https://dummy-verify-url.test

# AWS S3 (dummy for context loads)
aws.s3.access-key=${AWS_ACCESS_KEY:}
aws.s3.secret-key=${AWS_SECRET_KEY:}
aws.s3.region=${AWS_REGION:us-east-1}
aws.s3.bucket=${AWS_S3_BUCKET:dummy-bucket}

webhook.secret=${WEBHOOK_SECRET:test-webhook-secret}

spring.cache.type=simple
app.fe.url=${FRONTEND_URL:http://localhost:5173}